<Project>
  <!-- Keep this app self-contained and relocatable: never inherit strong-name
       signing props from a parent tree (e.g. cloned SDK repos). -->
  <PropertyGroup>
    <SignAssembly>false</SignAssembly>
    <DelaySign>false</DelaySign>
  </PropertyGroup>
</Project>
